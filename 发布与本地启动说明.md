# 马庆林作品集：发布与本地启动

## 一键本地启动

双击桌面的“启动马庆林作品集”快捷方式。它会：

1. 检查作品集服务器是否已经运行。
2. 如未运行，自动在后台启动 Vite。
3. 等待网站可访问后，自动打开浏览器。

不使用时，双击桌面的“关闭马庆林作品集”快捷方式即可结束后台服务器。

电脑访问地址：`http://127.0.0.1:5173/`

同一 Wi-Fi 下的手机访问地址：`http://192.168.1.216:5173/`

如果手机无法访问，请确认电脑和手机连接同一 Wi-Fi，并在 Windows 防火墙弹窗中允许 Node.js 访问“专用网络”。

## 推荐的公开发布方式

建议使用 Vercel。发布后电脑无需保持开机，并会得到一个 `*.vercel.app` 公网地址；购买域名后可在项目的 Domains 设置中绑定。

构建命令：`npm run build`

输出目录：`dist`

项目已经包含 `vercel.json`，可支持 `/project/66vip` 等独立项目地址直接访问与刷新。
